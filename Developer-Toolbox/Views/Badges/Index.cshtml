<section class="p-3">
    @if (TempData.ContainsKey("message"))
    {
        <h4 class="@ViewBag.MessageType p-3 rounded-3 text-center mb-5">@ViewBag.Message</h4>
    }
    <br />

@*     @if (ViewBag.EsteAdmin)
    {
        <div class="d-flex justify-content-end">
            <a class=" btn btn-success custom-btn mr-10" href="/Activities/Edit">Edit rewards</a>
        </div>

        <hr />
    } *@

    <h2 class="text-center"> Badges <i class="bi bi-clipboard2-check-fill"></i> </h2>
    <br />

    <div class="d-flex flex-column gap-2 align-content-center justify-content-center align-items-center">
        @foreach (var badge in ViewBag.Badges)
        {

            <div class="card w-75">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <h3 class="card-title primary py-3 px-3 rounded-2"> @badge.Title</h3>
                    <h3 class="card-title primary py-3 px-3 rounded-2"> @badge.Description</h3>
                </div>
            </div>
        }
    </div>

</section>