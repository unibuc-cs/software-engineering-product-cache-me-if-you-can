@model Developer_Toolbox.Models.Category


<section class="d-flex gap-2">
    <div class="w-50 m-5">
        <div class="card product">
            <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
                <div>
                    <div class="hover-overlay">
                        <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                    </div>
                </div>
            </div>

            <div class="card-body">
                <div href="" class="text-reset">
                    <img src="@Model.Logo" style="max-width:30px" />
                    <h5 class="card-title mb-3">@Model.CategoryName</h5>
                </div>
            </div>
            <div class="d-flex align-items-center justify-content-between m-2">

                @if (Model.UserId == ViewBag.CurrentUser || ViewBag.IsAdmin)
                {
                    <a class="btn alert-success" href="/Categories/Edit/@Model.Id">Edit Category</a>

                    <form method="post" action="/Categories/Delete/@Model.Id">

                        <button class="btn btn-danger" type="submit">Delete Category</button>

                    </form>
                }

            </div>

        </div>
    </div>
</section>

